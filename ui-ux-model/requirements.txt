# --- Dataset generation ---
openai>=1.30.0              # used for both Groq and OpenRouter (OpenAI-compatible endpoints)
anthropic>=0.40.0           # only needed if you use --provider anthropic
jsonschema>=4.21.0

# --- Fine-tuning (run these on your rented GPU, not locally) ---
unsloth @ git+https://github.com/unslothai/unsloth.git
torch>=2.3.0
transformers>=4.44.0
trl>=0.9.6
peft>=0.12.0
accelerate>=0.33.0
bitsandbytes>=0.43.0
datasets>=2.20.0

# --- RAG ---
sentence-transformers>=3.0.0
chromadb>=0.5.0

# --- Shared ---
tqdm>=4.66.0
